name: Run Autotests

on:
  workflow_dispatch:
    inputs:
      deployment_target:
        description: 'Select tests to run'
        required: true
        type: choice
        default: all
        options:
          - all

jobs:
  run-tests:
    runs-on: ubuntu-latest
    name: Run Tests
    
    steps:
      - name: Copy source code
        uses: actions/checkout@v4
      - name: Set up Python environment
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests with pytest
        run: pytest -v
      - name: Generate test report
        if: always()
        run: |
          echo "Tests completed!"
          echo "See report above for details."name: Run Autotests

on:
  workflow_dispatch:
    inputs:
      deployment_target:
        description: 'Select tests to run'
        required: true
        type: choice
        default: all
        options:
          - all
jobs:
  run-tests:
    runs-on: ubuntu-latest
    name: Run Tests
    
    steps:
      - name: Copy source code
        uses: actions/checkout@v4
      - name: Set up Python environment
        uses: actions/setup-python@v5 
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests with pytest
        run: pytest -v
      - name: Generate test report
        if: always()
        run: |
          echo "Tests completed!"
          echo "See report above for details."
